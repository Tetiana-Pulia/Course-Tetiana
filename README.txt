# Linux terminal (GitBash) commands 

Usage of the following commands: create a folder (mkdir), change folder (cd), create a file (touch), move file (mv), copy file (cp), list the files in a directory (ls, ls –la), read the beginning or the ending of the file (head or tail), modify files (vim), grep and curl commands, creating simple scripts
